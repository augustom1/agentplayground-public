Place your app screenshots here.

Recommended files:
  dashboard.png       — /dashboard page
  chat.png            — /chat page
  agent-lab.png       — /agent-lab page
  schedule.png        — /schedule page
  tools.png           — /tools page

Then replace the screenshot-placeholder divs in index.html with:

  <img src="assets/screenshots/dashboard.png" alt="Dashboard" class="screenshot-img" />

inside the corresponding .screenshot-frame elements.
